# NetSec Dispatch — Indico plugin.
#
# Intentionally has no i18n domain: this is an internal, single-operator
# plugin whose only UI is the admin settings form, so plain English
# strings are fine. If it ever grows user-facing copy, add a bound
# gettext here (see any official plugin's __init__.py for the pattern).
